// import { renderHook, act } from '@testing-library/react';
// import { BarProvider, useBar } from '../contexts/BarContext';

// const wrapper = ({ children }) => <BarProvider>{children}</BarProvider>;

describe('useBar Hook', () => {
  it('should have a placeholder test for initial state', () => {
    // const { result } = renderHook(() => useBar(), { wrapper });
    // expect(result.current.selectedBar).toBe('all');
    // expect(result.current.viewingCuratedMenu).toBeNull();
    expect(true).toBe(true);
  });

  // TODO: Test selectBar function
  // TODO: Test viewCuratedMenu function (and its effect on selectedBar)
});
